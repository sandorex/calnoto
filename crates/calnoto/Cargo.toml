[package]
name = "calnoto"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
# use godot installed so i don't have to specify version twice
godot = { version = "0.4.5", features = [ "api-4-5" ] }
