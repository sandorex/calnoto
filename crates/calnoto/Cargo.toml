[package]
name = "calnoto"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
godot = { version = "0.4.5", features = [ "api-4-5" ] }
git2 = { version = "0.20.4", features = [ "vendored-libgit2", "vendored-openssl" ] }

